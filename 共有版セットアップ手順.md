# 全員でデータ共有 ― セットアップ手順（初回のみ・約10分）

Googleスプレッドシートを「みんなの保存先」にして、全員が同じ商品の進捗を読み書きできるようにします。
アプリ側（`index.html`）は準備済みです。あとは **Google側の設定を1回だけ** 行い、アプリに「接続先URL」と「合言葉」を入れるだけです。

> 仕組みは社内の他アプリ（商品開発管理・工場課題アプリ等）と同じです。すでに設定した経験がある方は、同じ流れで進められます。

---

## STEP 1. 保存先のスプレッドシートを作る

1. https://sheets.new を開く（新しい空のスプレッドシートができます）
2. 名前を「**新商品導入フロー_共有データ**」などに変更（任意）

※ このシートには自動でデータ（JSON）が書き込まれます。**中身を手で編集する必要はありません。**

## STEP 2. スクリプトを貼り付ける

1. スプレッドシートのメニュー **拡張機能 → Apps Script** を開く
2. 最初から入っているコードを全部消す
3. このフォルダの **`共有サーバー_AppsScript.gs`** の中身を全部コピーして貼り付け
4. 先頭近くの
   ```
   var TOKEN = 'donyu-2026-CHANGE-THIS';
   ```
   の `donyu-2026-CHANGE-THIS` を、**あなただけが決める合言葉**に書き換える
   （例：英数字20文字くらい。人に推測されないもの。この合言葉は後で使います）
5. 💾 保存（フロッピーのアイコン）

## STEP 3. 「ウェブアプリ」として公開する

1. 右上の **デプロイ → 新しいデプロイ**
2. 歯車⚙アイコン →「**ウェブアプリ**」を選択
3. 設定：
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
     （※URLと合言葉を知っている人しか使えないので、この設定で問題ありません）
4. **デプロイ** を押す
5. 「アクセスを承認」→ 自分のGoogleアカウントを選択
   → 「このアプリは Google で確認されていません」と出たら
   **詳細 →（安全でないページ）に移動 → 許可**（自分で作ったスクリプトなので安全です）
6. 表示される **ウェブアプリのURL**（`https://script.google.com/macros/s/……/exec` で終わるもの）を**コピー**

## STEP 4. アプリに接続する（管理者＝あなた）

1. **手元の `index.html`**（今のデータが入っているもの）をブラウザで開く
2. 右上の「**共有**」ボタンを押す
3. STEP3でコピーした **URL** と、STEP2で決めた **合言葉** を入力
4. 「**接続テスト**」→「接続OK」が出ればOK
5. 「**この端末の商品を共有データに追加（未共有分のみ）**」を押す
   → 今の商品一覧・進捗がスプレッドシートに保存されます

これで準備完了です。以後、あなたの編集は自動でサーバーに保存されます。

## STEP 5. 他のメンバーに使ってもらう

メンバーには、次の3つを伝えます。

1. アプリのURL： **（GitHub PagesにデプロイしたURL。例：https://ユーザー名.github.io/リポジトリ名/ ）**
2. 接続先URL（STEP3のURL）
3. 合言葉（STEP2で決めたもの）

メンバーの操作：
1. アプリのURLを開く
2. 右上「**共有**」→ 接続先URLと合言葉を入力 →「**接続して読み込む**」
3. 全員のデータが表示されます。編集すると自動でみんなに反映されます

> まだGitHub Pagesに公開していない場合は、`index.html` ファイルそのものを配ってもローカルでは使えますが、**データ共有には全員がSTEP3のURL＋合言葉を入れる必要があります**（アプリのURLがローカルでも、接続先が同じなら同期します）。全員で使うなら、社内の他アプリと同じくGitHub Pagesでの公開をおすすめします。

---

## 使い方・注意

- **画面上部の丸いバッジ（共有の状態）**
  - **共有中**（緑）… サーバーと同期しています
  - **保存中／読み込み中**（黄）… 通信中
  - **同期エラー／保存エラー**（赤）… 通信できていません（自動で再試行します）
- **同時に編集しても大丈夫です。** 別々の人が同じ商品を編集しても、互いの入力は自動で合わさります（同じ欄を同時に変えた場合のみ、後から保存した方が残ります）。
- 画面を開いている間は**15秒ごとに最新を自動で取り込みます**。バッジをクリックすれば、いつでもすぐ最新を取り込めます。
- **「この端末は共有に接続されていません」という黄色の表示が出ている端末**は、入力が他の人に見えません。共有リンクを開き直すか、「管理」→「共有の設定」から接続してください。接続時、その端末だけに保存されていた商品があれば、共有データへ追加するか確認が出ます。
- **合言葉とURLは社外に出さないでください。** これがアクセスの鍵です。漏れた場合は STEP2 の合言葉を変えて STEP3 で再デプロイしてください。

## こまったとき

- 「接続できません」→ URLが `…/exec` で終わっているか、合言葉が一致しているか確認。
- スクリプトを直したら、**デプロイ → デプロイを管理 → 編集（鉛筆）→ バージョン：新バージョン → デプロイ** で更新（URLは変わりません）。
  「新しいデプロイ」で作り直すとURLが変わるので注意。

---

## このアプリの中身（参考）

- **商品ごとにカードで一覧**。カードをクリックすると、右から詳細（3フェーズ・15工程）が開きます。
- 商品ごとに **商品ステータス**（構想中／提案中／採用／不採用）を設定できます。不採用の商品は一覧で灰色・進捗0%表示になります。
- 各工程の **ステータス**（未着手／進行中／完了）を選ぶと、進捗率と現在フェーズが自動更新されます。
- **担当・依頼日・〆切・備考** も工程ごとに記録できます。一覧カードの **最新状況** はその場で編集できます。
- 右上「管理」の「書き出し／読み込み」で、JSONバックアップの保存・復元ができます（共有とは別に、手元保存用）。
